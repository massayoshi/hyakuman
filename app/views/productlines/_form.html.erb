<%= form_for(@productline) do |f| %>
  <% if @productline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@productline.errors.count, "error") %> prohibited this productline from being saved:</h2>

      <ul>
      <% @productline.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :manufacturer_id %><br />
	<%= f.collection_select :manufacturer_id, Manufacturer.order(:name), :id, :name, include_blank: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
